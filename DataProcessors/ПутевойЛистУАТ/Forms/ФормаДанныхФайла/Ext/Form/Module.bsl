
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	Если Параметры.Свойство("РезультатЗагрузки") Тогда
		Для Каждого Стр Из Параметры.РезультатЗагрузки Цикл
			ЗаполнитьЗначенияСвойств(ДанныеФайла.Добавить(), Стр);
		КонецЦикла;		
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПеренестиВДокумент(Команда)
	ВыбранныеСтроки = ДанныеФайла.НайтиСтроки(Новый Структура("Флаг", Истина));
	ОповеститьОВыборе(ВыбранныеСтроки);
КонецПроцедуры
